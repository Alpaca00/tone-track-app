import 'package:flutter_test/flutter_test.dart';

import 'package:tone_track/main.dart';

void main() {
  testWidgets('Create entry - smoke test', (WidgetTester tester) async {
    await tester.pumpWidget(const ToneTrackApp());

  // todo: impl create, view and remove entry
  });
}
